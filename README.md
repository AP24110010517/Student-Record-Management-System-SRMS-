# Student-Record-Management-System-SRMS-
A C-based Student Management System using file handling and role-based login (Admin, Staff, User, Guest). Supports adding, viewing, searching, updating, and deleting student records via text files, with secure authentication and menu-driven operations for easy academic use.
